[[bin]]
name = "cc-json-parser"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }

[lib]
path = "src/lib.rs"

[package]
name = "cc-json-parser"
version = "0.1.0"
edition = "2024"
license = "MIT"
